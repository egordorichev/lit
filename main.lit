require("http")

HTTP.get("https://www.reddit.com/r/memes.json", {
	sort: "new"
}).then((response) => {
	print(response)
	var data = response.body.data.children
	print(data[Random.int(data.length)].url)
}).catch((err) => {
	print("Error: " + err)
})