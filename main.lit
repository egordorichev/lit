require("http")

HTTP.get("http://www.reddit.com/r/memes.json?", {
	sort: "new"
}).then((response) => {
	print(response.body)
	var data = response.body.data.children
	print(data[Random.int(data.length)].url)
}).catch((err) => {
	print("Error: " + err)
})