class Test {
	operator == (value) {
    return 2 == value
  }
}

var t = new Test()
print(t == 3)
print(t == 2)

// todo: if argument expected type doesnt match in native function, like r  equire, it will crash (cuz it still continues to operate)