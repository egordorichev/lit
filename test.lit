// fixme: handle_runtime_error doesn't really work yet, RESTORE_FRAME() stuff is not being called obv
print(new Fiber(() => {
  null()
}).try()) // Expected: Attempt to call a null value

// fixme: calling !instance will try to run method ! and when it doesnt find it just continue